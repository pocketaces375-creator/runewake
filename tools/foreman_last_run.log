=== foreman: 2026-08-14T13:11:31Z TASK-DSL-3 retry ===
Session: 7/16
Failures: checkbox_not_flipped 
--- session tail ---
Here's what I've accomplished and what remains:

## TASK-DSL-3 Implementation Status

### ✅ Completed

**Engine changes:**
- `CardEnums.cs` — Added `COST_MOD` to the `Op` enum
- `EffectDef.cs` — Added `AppliesTo`, `Value`, `Stacks`, `Floor` fields
- `CostMod.cs` (new) — Runtime data structure with `Clone()`, all required fields (applies_to, filter, filter_value, condition, duration, stacks, amount, floor, used_this_turn, source tracking)
- `PlayerState.cs` — Added `CostMods` list with `Clone()` support
- `EffectExecutor.cs` — Added `ApplyCostMod()` handler (registers/replaces cost mods on player target, stacking or non-stacking per source artifact)
- `DuelEngine.cs` — Added `GetEffectiveCost()`, `ConsumeCostMods()`, `RefreshCostMods()` methods; modified `ApplyPlayCard` to use effective discounted cost; called `RefreshCostMods` at turn start (removes THIS_TURN mods, resets per-turn usage flags); called `ConsumeCostMods` after successful card play (marks FIRST_SPELL_EACH_TURN as used)
- `RulesTextRenderer.cs` — Added COST_MOD rendering
- `Bot.cs` — `EnumerateValidActions` uses `GetEffectiveCost`
- `GameState.cs` — Added `HashCostMod` to state hash + `CostMods` fields copied in EffectDef copy sites

**Content migration:**
- `launch_artifacts.json` — All 4 ATTUNE discounts migrated to COST_MOD:
  - Wand (Mage): `FIRST_SPELL_EACH_TURN` passive
  - Aura (Mage): `THIS_TURN` + `stacks: true` trigger
  - Duskfang (Thief): `ATTACK_LTE` passive
  - Skull (Necromancer): `CREATURE_DIED_THIS_TURN` condition passive

**Tests:**
- `CostModTests.cs` (25 tests, 22 passing) — Covers: generic discount, floor 0, applies_to (CREATURE/SPELL), ATTACK_LTE filter, FIRST_SPELL_EACH_TURN per-turn, condition gating, stacking, UsedThisTurn skip, integration with ApplyPlayCard, insufficient attunement, full discount to 0, lifecycle (THIS_TURN removal, UsedThisTurn reset), Wand/Aura/Grimoire patterns, clone correctness, hash inclusion, Bot action enumeration

### ❌ 3 Tests Failing (stale reference bug)

All 3 failures share the same root cause: after `state = DuelEngine.Apply(state, ...)` returns a cloned state, the tests still reference the old `player` variable:

1. **`PlayCard_UsesDiscountedCost`** (line 225) — `player` variable stale after clone
2. **`Aura_StacksThisTurn_MultipleDiscountsAccumulate`** (line 442) — Same stale reference
3. **`Wand_FirstSpellEachTurn_WorksViaFullFlow`** (line 400) — Same

Fix: re-assign `player = state.Players[0]` after each `DuelEngine.Apply` call (already fixed in `PlayCard_ConsumesFirstSpellEachTurn`). Three more occurrences need the same fix in the test file.

### Next step

Quick fix of the 3 stale references in `CostModTests.cs`, then:
```bash
dotnet test tests/ --filter "FullyQualifiedName~CostModTests" -v n
git add -A && git commit -m "TASK-DSL-3: COST_MOD op (discount mechanic)..."
git push
```
========================================
