TASK-DSL-4 done. Protocol followed: implement → tests → commit → push → DONE line → stop.

Implementation:
- EffectDef: new `Cadence` + `Order` fields (JSON `cadence`/`order`) with constants `ON_TURN_START` and `BEFORE_ALL_OTHER_TURN_START_EFFECTS`
- DuelEngine: new turn-start cadence phase (3.5, after Attune, before Draw) — `FireCadencedPassives()` sorts cadenced artifact passives by the explicit ordering key (BEFORE_ALL_OTHER first, stable by slot) so Bow Prey marking resolves before Censer heal, both before draw (R11, R15). `ApplyArtifactPassives` now skips cadenced effects to avoid double-fire
- TargetResolver: added `MOST_WOUNDED` filter (Censer's R11 target)
- CardInstance: Clone() now copies Cadence/Order (was dropping them on state clone — caught by tests)

Tests: 9 new in tests/Engine/CadencePassiveTests.cs pinning order (ordering-key interaction test, reversed-order negative, real Bow+Censer pair, no-enemy/no-mark R15, no-wound/no-heal R11, cadence-before-trigger-phase via hand-size watcher, suppression R18/G3, JSON wiring).

Verification: 540/540 tests pass (9 new + 531 legacy). Commits `e7c8d35` + `b235d1f` pushed to main. TASKS_QUEUE.md marked [x], DONE line in HERMES_STATUS.md. Stopping here per protocol.
========================================
=== foreman: 2026-08-14T14:24:35Z TASK-DSL-5 success ===
Session: 10/16
--- session tail ---
  Bitwarden Secrets Manager: secrets.bitwarden.enabled is true but BWS_ACCESS_TOKEN is not set.  Run `hermes secrets bitwarden setup`.
Done. TASK-DSL-5 complete:

**Implementation** (commit `7264b41` + DONE line `014887f`, pushed to main):
- `Op.RESET_CHARGES` added; `ArtifactSlot` gains per-turn tracking (`ChargesGainedThisTurn`, per-creature dict, `PendingChargeFull`) plus config-derived caps and `HasDeferredChargeFull` from `ChargeConfig.max_per_turn` / `max_per_creature_per_turn` and `AbilityDef.Timing="END_OF_TURN"` — all already declared in launch_artifacts.json (Censer, Duskfang, Grimoire).
- `EffectExecutor.ApplyAddCharge` enforces both caps, freezes charges under suppression (G3), fires ON_CHARGE_FULL immediately or defers to end of turn; `RESET_CHARGES` case added.
- `DuelEngine.ApplyEndTurn` fires deferred ON_CHARGE_FULL (G8) and resets per-turn charge tracking at owner's turn start.
- State hash + clone cover all new fields for deterministic replay; RulesTextRenderer renders "Reset Charges".

**Verification:** 568/568 tests green (28 new ChargeTests + 540 legacy). TASKS_QUEUE.md marked `[x]`, DONE line written to HERMES_STATUS.md. Stopping here per protocol.
========================================
=== foreman: 2026-08-14T15:11:20Z TASK-DSL-6 success ===
Session: 11/16
--- session tail ---
  Bitwarden Secrets Manager: secrets.bitwarden.enabled is true but BWS_ACCESS_TOKEN is not set.  Run `hermes secrets bitwarden setup`.
Done. Here's the summary of what was implemented:

**Engine changes:**
- **`CardEnums.cs`** — Added `FORGE` to `Op` enum
- **`EffectDef.cs`** — Added `SpendFrom`, `Spend`, `PerCharge` fields for FORGE-specific config
- **`PerChargeStats.cs`** (new) — Attack/Vigor per-charge stats model
- **`TargetDef.cs`** — Added `Tiebreak` field
- **`TargetResolver.cs`** — Integrated tiebreak into all sort filters (`HIGHEST_COST` + `OLDEST_IN_PLAY` gives correct ordering by cost desc then instanceId asc for tiebreaking)
- **`EffectExecutor.cs`** — `ApplyForge` implementation: finds partner slot (twin artifact), spends ALL charges, applies per-charge attack/vigor buff, keeps charges if no creature target, respects suppression

**Tests — `ForgeTests.cs`** (19 tests):
- PARTNER_CHARGES_GTE condition: met, not met, zero, max
- FORGE: spends all charges, buffs per charge, targets highest cost creature
- Tiebreak: OLDEST_IN_PLAY with 2 and 3 creatures at same cost
- Edge cases: no creature (keeps charges), zero charges, suppressed partner, enemy-only creatures
- Per-charge variants: attack-only, vigor-only, linear scaling with many charges
- Full trigger integration: fires through ON_TURN_END_NO_ATTACK correctly

**Results:** All 587 tests pass (463 legacy + 19 new). Committed and pushed.
========================================
