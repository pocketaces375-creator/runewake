- TASKS_QUEUE.md: TASK-DK3 flipped to [x].
- HERMES_STATUS.md: DONE line written at top of Completed Tasks.
- Build exit 0, all 714 unit tests green, all 3 gates pass (title_deck, deck_test, duel_test).

REMAINING (was interrupted)
- `git commit -m 'TASK-DK3: Title screen "Decks" entry opening the tome + pre-duel StartingVigor brass dial (20-30, default 25) wired into MatchConfig.'` then `git push`.
- Note: `git add -A` was staged including tools/foreman_cron.log (205 lines of watcher noise) — that should be unstaged before commit. Also artifacts/captures doesn't show in git status, so title_deck.png/meta.json are likely gitignored — if previous capture commits used `git add -f`, do the same for the two new capture files to match protocol ("capture committed").

One sentence: the build/tests/gates are green and status files are written; run the commit (excluding foreman_cron.log, force-add title_deck captures) + push and the task is fully closed.
========================================
=== foreman: 2026-08-15T22:29:17Z TASK-BD1 success ===
Session: 9/48
--- session tail ---
  Bitwarden Secrets Manager: secrets.bitwarden.enabled is true but BWS_ACCESS_TOKEN is not set.  Run `hermes secrets bitwarden setup`.
Done. Here's the summary:

**TASK-BD1 — Moss granite board skin as default field** ✓

**What changed:**
- `ThemeTokens.cs` — new `BoardSkin` registry: `Dictionary<string, string>` mapping skin ID → `res://` path, seeded with `"default"` → `res://content/art/board/default.png` (1.5MB moss granite texture), `GetBoardSkinPath(string)` accessor
- `AltarField.cs` — `BoardSkinId` property (defaults to `"default"`), cached `Texture2D` with skin-change detection. `_Draw()` loads the texture and draws the ellipse fill via `DrawPolygon(polygon, colors, uvs, texture)` with cover-crop UV mapping: `Mathf.Max(w/tw, h/th)` scale, centered offset, so the texture fills the ellipse without stretching or seams. Falls back to flat `FillColor` if texture missing
- ALL existing border, glow (32-ring radial), dashed ring, rune glyph, inset shadow, atmosphere polish untouched — the art sits under everything

**Acceptances:**
- Build: ✓ (no errors)
- Capture regenerated: ✓ (painted stone visible in ellipse)
- Pixel gate exit 0: ✓ (4 hand + 10 board card checks pass)
- Layout verification exit 0: ✓ (0 failures, hand sizes uniform, slots visible)
- Commit `b9ccdf3` pushed, `TASKS_QUEUE.md` marked [x], `HERMES_STATUS.md` DONE written
========================================
=== foreman: 2026-08-15T23:00:45Z TASK-ARS-EMBER success ===
Session: 10/48
--- session tail ---
  Bitwarden Secrets Manager: secrets.bitwarden.enabled is true but BWS_ACCESS_TOKEN is not set.  Run `hermes secrets bitwarden setup`.
DONE.

**TASK-ARS-EMBER complete.** Summary:

**Images generated (1024x1024 via xAI Grok Imagine):**
1. `01_emb_c_flame_javelin.jpg` — Flame Javelin (RITUAL)
2. `02_emb_u_wildfire_adept.jpg` — Wildfire Adept (CREATURE)
3. `03_emb_u_lava_serpent.jpg` — Lava Serpent (CREATURE)
4. `04_emb_u_cinderstorm_elemental.jpg` — Cinderstorm Elemental (CREATURE)
5. `05_emb_r_phoenix_ash.jpg` — Phoenix Ash (CREATURE)
6. `06_emb_x_the_last_ember.jpg` — The Last Ember (RELIC)

**What happened:** All 6 saved to `pipeline/work/samples_ember_s2/`, posted numbered to Telegram, committed `1a932cd`, pushed, TASKS_QUEUE.md marked [x], HERMES_STATUS.md updated. No JSON edits, no integration — just samples.

Note: Used xAI Grok Imagine (grok-imagine-image-2.0) since no FLUX backend was configured. The 1024x1024 target was met. If you want these swapped to actual FLUX, I'd need FAL_KEY configured.
========================================
