All effects: <400ms, skippable via `CampaignContext.ReduceMotion`, no layout changes, all 825 tests pass, ui_lint pre-existing failures only.
========================================
=== foreman: 2026-09-04T13:53:00Z TASK-WARDEN-RULE-1 success ===
Session: 3/96
--- session tail ---
  TASK-WARDEN-RULE-1: attempt 1 transient — cleaning dead-session tree litter
Removing tmp/boss_deck.json
Removing tmp/player_deck.json
  TASK-WARDEN-RULE-1: attempt 2/3 — waiting 60s before retry
  Bitwarden Secrets Manager: secrets.bitwarden.enabled is true but BWS_ACCESS_TOKEN is not set.  Run `hermes secrets bitwarden setup`.
Here's what I've accomplished so far:

**Engine primitive (opening_rule):**
- Added `opening_rule` field to `EncounterDef.cs` — boss encounters can declare one
- Added `OpeningRule` to `GameConfig.cs` — flows through to `GameState.Initialize`
- Added `OpeningRule`, `OpeningRuleLifted[]`, `TotalCreatureDiedCount[]` to `GameState.cs`
- Added `IsBuried` to `LaneState.cs` — lanes can be blocked
- Created `OpeningRuleHandler.cs` — applies rules at init, checks lift conditions on creature death

**Root-choked rule:**
- Buries P0's lane 0 (leftmost) at game start
- Lifts when the Warden (P1) loses their first creature (any creature, any cause)
- Enforced in `DuelEngine.ApplyPlayCard` — throws if a buried lane is targeted

**Banner card UI:**
- Created a dark-green styled `PanelContainer` banner at the top of the board in `DuelScene._Ready`
- Shown/hidden in `OnStateChanged` based on `state.OpeningRule`
- Displays "Root-choked — your leftmost lane is buried..." in pale green, switches to "[LIFTED] ..." when the condition is met

**JSON wiring:**
- Added `"opening_rule": "root_choked"` to the `r1_boss_warden_aelin` encounter in `region_01_boss.json`
- No other encounter gets a rule

**Sim support:**
- Added `OpeningRule` to `BatchConfig` and `BatchRunner.Run` — the sim pipeline passes it through

**Verification:**
- Engine builds: 0 errors, 4 warnings (pre-existing)
- Sim builds: 0 errors
- All 825 existing tests pass

**Remaining (ran out of iterations):**
1. Rebuild tests (`dotnet build tests/`) — I fixed the artifact loading path in the test file
2. Run the sim test to get before/after win% (`dotnet test tests/ --filter BossDeck_WinPercent_WithOpeningRule`)
3. Run loop_smoke to verify it still passes
4. Take a capture of the banner in the boss fight
5. Commit with message, push, write DONE line in HERMES_STATUS.md

The test file at `/home/fictive/runewake-lane4/tests/Campaign/BossOpeningRuleSimTests.cs` runs 200 sims without the rule, then 200 with root_choked, and reports the delta. Need to rebuild and run it.
========================================
