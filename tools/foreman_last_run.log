
**TASK-PIPE-IMPORT-1: import before capture**

Changes:
- `client/scripts/RootBoundBorder.cs` — added `GD.Print` after `bandPx` compute: `[ROOTBOUND] Setup cardWidth=W cardHeight=H bandPx=B`
- `tools/finish_task.sh` — added `rm -rf .godot/imported/` + `xvfb-run godot --headless --import` before the capture loop, with hard fail on non-zero exit
- `tools/capture_duel.sh` — same import step before all `capture_one` calls

Verification — `finish_task.sh` exited 0 through all steps (build, art_check, tests, capture regen, ui_lint, label_fit, commit).

[ROOTBOUND] Loaded lines from duel_test capture (all 8 present):
```
[ROOTBOUND] Loaded res://content/art/border/rootbound_corner_tl.png
[ROOTBOUND] Loaded res://content/art/border/rootbound_corner_tr.png
[ROOTBOUND] Loaded res://content/art/border/rootbound_corner_bl.png
[ROOTBOUND] Loaded res://content/art/border/rootbound_corner_br.png
[ROOTBOUND] Loaded res://content/art/border/rootbound_edge_top.png
[ROOTBOUND] Loaded res://content/art/border/rootbound_edge_bottom.png
[ROOTBOUND] Loaded res://content/art/border/rootbound_edge_left.png
[ROOTBOUND] Loaded res://content/art/border/rootbound_edge_right.png
```

[ROOTBOUND] Setup lines — hand card and board card:
```
  Hand card:  [ROOTBOUND] Setup cardWidth=232.63158 cardHeight=340 bandPx=16
  Board card: [ROOTBOUND] Setup cardWidth=199.69032 cardHeight=292 bandPx=14
```

Commits: `5be70e5` (import before capture) + `b20d037` (mark [x] + DONE entry). Task marked done in queue.
========================================
=== foreman: 2026-09-09T13:36:09Z TASK-PIPE-INBOX-1 success ===
Session: 2/96
--- session tail ---
  session TASK-PIPE-INBOX-1 on deepseek/deepseek-v4-flash: 310s
Task TASK-PIPE-INBOX-1 is complete. Commit `77fa3f4`, mark commit `92d3364`.

**What changed in `tools/inbox_apply.sh`:**

1. **Header comment** (line 8) — updated to say "moved to needs_reformat/ for Fable to fix" instead of "run as one-shot hermes prompt"

2. **Entire else-branch** (lines 181-187) — replaced 42 lines of hermes prompt-passing code with:
   - `mkdir -p "${INBOX_DIR}/needs_reformat"` — creates the drop directory
   - `mv "${inbox_file}" "${INBOX_DIR}/needs_reformat/"` — moves the malformed file out of inbox
   - Log line with timestamp explaining the action
   - Telegram notification via `"${HERMES_BIN}" send --to` using the foreman's `TELEGRAM_TARGET` pattern: *"Inbox file <name> is not pure queue format — moved to needs_reformat/, nothing was run. Fable: resend as - [ ] TASK blocks with indented continuation lines."*

3. **Common log label** (line 193) — changed from `'one-shot'` to `'needs-reformat'` since that's the only remaining outcome for non-queue files

4. **Atomic edit** — written to `.tmp` file then `mv` over the original to avoid cron seeing a half-written file
========================================
