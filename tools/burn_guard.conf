# Runewake burn guard — edit and commit; burn_guard.sh reads this every run
DAILY_CAP=15        # dollars/day on the OpenRouter key before every lane halts
HOURLY_CAP=2.5      # dollars/hour sustained for 20 min = runaway
FAIL_STREAK=5       # this many consecutive failed lane sessions = gate is broken, halt
CANARY_EVERY=3600   # seconds between clean-tree gate checks
WARN_AT=0.75        # warn in the group at this fraction of the daily cap
